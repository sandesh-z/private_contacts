import 'package:flutter/material.dart';

class PrivateContactApp extends StatelessWidget {
  const PrivateContactApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
